// Insertion sort is a simple sorting algorithm that works similar to the way you sort playing cards in your hands.
// The array is virtually split into a sorted and an unsorted part.
// Values from the unsorted part are picked and placed at the correct position in the sorted part.

// Characteristics of Insertion Sort:
// This algorithm is one of the simplest algorithm with simple implementation
// Basically, Insertion sort is efficient for small data values
// Insertion sort is adaptive in nature, i.e. it is appropriate for data sets which are already partially sorted.


// Sắp xếp chèn là một thuật toán sắp xếp đơn giản hoạt động tương tự như cách bạn sắp xếp các thẻ chơi trong tay của mình.
//  Mảng hầu như được chia thành một phần được sắp xếp và một phần chưa được sắp xếp.
//  Các giá trị từ phần chưa được sắp xếp được chọn và đặt ở vị trí chính xác trong phần được sắp xếp.

// Đặc điểm của Sắp xếp Chèn:
// Thuật toán này là một trong những thuật toán đơn giản nhất với cách thực hiện đơn giản
// Về cơ bản, sắp xếp chèn hiệu quả đối với các giá trị dữ liệu nhỏ
// Sắp xếp chèn có bản chất thích ứng, tức là nó thích hợp cho các tập dữ liệu đã được sắp xếp một phần.