// Heap sort is a comparison - based sorting technique based on Binary Heap data structure. 
// It is similar to selection sort where we first find the minimum element and place the minimum element at the beginning.We repeat the same process for the remaining elements.

// Heap sort là một kỹ thuật sắp xếp dựa trên so sánh dựa trên cấu trúc dữ liệu Binary Heap. 
// Nó tương tự như sắp xếp lựa chọn trong đó trước tiên chúng ta tìm phần tử tối thiểu và đặt phần tử tối thiểu ở đầu.Chúng tôi lặp lại quá trình tương tự cho các phần tử còn lại.